import { Box, Typography } from '@mui/material';

const ProfileCard = () => {
  return (
    <Box>
      <Typography>Profile Card Component</Typography>
    </Box>
  );
};

export default ProfileCard;
